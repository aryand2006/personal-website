GITHUB_USERNAME=aryand2006
# Recommended on Vercel so live deploys refresh the repo list.
# Public repos only need a fine-grained token with Contents: Read,
# or a classic token with public_repo. Without it, the site uses
# lib/fallback-repos.ts when GitHub rate-limits the request.
GITHUB_TOKEN=
